{
  "name": "blackjack-dealer-logic",
  "version": "1.0.0",
  "description": "A library for black jack dealer logic",
  "main": "index.js",
  "repository": "fix.this.immediately",
  "author": "Don Hamilton III <donhamiltoniii@gmail.com>",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "@types/readline-sync": "^1.4.3",
    "playing-card-deck-generator": "^1.5.0",
    "readline-sync": "^1.4.10"
  },
  "scripts": {
    "build": "tsc",
    "lint": "tslint -c tslint.json 'lib/**/*.{ts,tsx}'",
    "pretest": "yarn run lint --fix",
    "start": "yarn build && node dist/ui.js",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@types/jest": "^24.0.18",
    "@types/node": "^12.7.2",
    "jest": "^24.9.0",
    "ts-jest": "^24.0.2",
    "tslint": "^5.19.0",
    "typescript": "^3.5.3"
  },
  "jest": {
    "moduleFileExtensions": [
      "ts",
      "tsx",
      "js"
    ],
    "transform": {
      "\\.(ts|tsx)$": "ts-jest"
    }
  }
}
